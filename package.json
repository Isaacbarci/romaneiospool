{
  "name": "romaneiospool",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx http-server public/ -p 8080",
    "build": "node generateVersion.js && npx http-server public/",
    "start": "npx http-server public/",
    "vercel-build": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
